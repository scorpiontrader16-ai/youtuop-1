apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: control-plane
  namespace: platform
  labels:
    app: control-plane
spec:
  minAvailable: 0
  selector:
    matchLabels:
      app: control-plane
