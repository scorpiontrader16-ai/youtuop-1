apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: control-plane
  namespace: platform
  labels:
    app: control-plane
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: control-plane
