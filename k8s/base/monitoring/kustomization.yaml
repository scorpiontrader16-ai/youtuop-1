# ╔══════════════════════════════════════════════════════════════════════╗
# ║  Full path: k8s/base/monitoring/kustomization.yaml                  ║
# ║  Status: ✏️ Modified — Deployment→StatefulSet + Falco networkpolicy  ║
# ╚══════════════════════════════════════════════════════════════════════╝
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # ── Core monitoring stack ─────────────────────────────────────────────────
  - stack.yaml
  - alertrules.yaml
  - slo.yaml
  - dashboards-configmap.yaml
  # ── AlertManager (StatefulSet HA) ─────────────────────────────────────────
  - alertmanager-externalsecret.yaml
  - alertmanager-configmap.yaml
  - alertmanager-statefulset.yaml      # Replaced alertmanager-deployment.yaml
  - alertmanager-service.yaml
  - alertmanager-networkpolicy.yaml    # GAP-03: ingress from falco namespace
  # ── VMAlert ───────────────────────────────────────────────────────────────
  - vmalert-serviceaccount.yaml
  - vmalert-rules-configmap.yaml
  - vmalert-deployment.yaml
  - vmalert-service.yaml
  # ── Pyroscope Continuous Profiling ─────────────────────────────────────────
  - pyroscope.yaml
