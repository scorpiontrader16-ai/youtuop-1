apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: notifications
  namespace: platform
  labels:
    app: notifications
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: notifications
